#!/bin/sh
# TEMPORARY. This file exists only to make scripts/check-workflows.cljs fail on
# one CI run, so the previous commit can be shown to work: with step 10 red, the
# twenty-six steps behind it must still run. Removed in the next commit.
exit 0
