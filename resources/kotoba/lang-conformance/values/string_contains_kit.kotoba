(ns kotoba.lang.conformance.string-contains-kit)

;; T1.3 expand: string-contains? on dual backends (i64 1/0).
(defn main []
  (+ (string-contains? "final decision" "decision")
     (string-contains? "final decision" "banana")))
