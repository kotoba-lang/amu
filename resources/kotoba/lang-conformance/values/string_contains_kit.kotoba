(ns kotoba.lang.conformance.string-contains-kit)

;; T1.3 expand: bool predicates converted explicitly at the numeric boundary.
(defn main []
  (+ (if (string-contains? "final decision" "decision") 1 0)
     (if (string-contains? "final decision" "banana") 1 0)))
