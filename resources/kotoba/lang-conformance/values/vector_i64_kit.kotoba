(ns kotoba.lang.conformance.vector-i64-kit (:export [main]))
;; T4.5 / T1.3: bounded vector-i64 dual-backend (count/at/conj).
(defn main [] :i64
  (let [v (vector-i64 10 20 30)]
    (+ (vector-count v)
       (vector-at v 1)
       (vector-count (vector-conj v 40)))))
