(ns kotoba.lang.conformance.pair-list-kit (:export [main]))
;; T1.3: pair/cons/first/empty? dual-backend (untagged pair-chain).
(defn main [] :i64
  (+ (pair-first (pair 3 4))
     (+ (first (cons 9 0))
        (+ (if (empty? 0) 1 0)
           (if (empty? (cons 1 0)) 0 2)))))
