(ns kotoba.lang.conformance.string-length-concat-kit (:export [main]))
;; T1.3/T4.2: string-length alias + string-concat dual-backend.
(defn main [] :i64
  (+ (string-length "ab")
     (+ (string-byte-length "cd")
        (string-byte-length (string-concat "x" "yz")))))
