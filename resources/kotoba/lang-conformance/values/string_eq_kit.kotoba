(ns kotoba.lang.conformance.string-eq-kit)

;; T1.3 expand: string=? equality + concat dual backends.
(defn main []
  (if (string=? "ab" (string-concat "a" "b"))
    (if (string=? "ab" "ac") 99 2)
    1))
