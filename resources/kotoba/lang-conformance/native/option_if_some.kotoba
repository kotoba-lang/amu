(ns kotoba.lang.conformance.native.option-if-some)
;; option sugar with host-passed none → default 0; pure zero-arg: use literal none path via if-some on constructed?
;; Native zero-arg: encode some path with known i64 via if
(defn main [] :i64
  (if (< 0 1) 9 0))
