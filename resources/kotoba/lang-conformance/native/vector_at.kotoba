(ns kotoba.lang.conformance.native.vector-at)
;; Indexed read. The middle element is chosen deliberately: a lowering that
;; confused the slice offset with the element index would still return the
;; right answer for index 0.
(defn main [] :i64
  (nth [10 20 30] 1))
