(ns kotoba.lang.conformance.native.vector-assoc-at)
;; Replace an element, then read it. Unlike conj this always copies, since the
;; changed word sits inside a slice other handles may span.
(defn main [] :i64
  (vector-at (vector-assoc (vector-new 1 2 3) 1 99) 1))
