(ns kotoba.lang.conformance.when-cond-kit (:export [main]))
;; T1.3: when + cond + if-let pure-product control sugar.
(defn main [] :i64
  (+ (when (< 1 2) 9)
     (cond (< 3 1) 1
           (< 0 1) 5
           :else 0)
     (if-let [x 7] x 0)))
