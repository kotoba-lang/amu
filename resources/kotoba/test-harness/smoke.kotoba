(ns kotoba.test-harness.smoke
  (:export [test-add test-string]))

;; T9.3 harness: exported zero-arity test-* fns return i64 1 on pass.
(defn test-add [] :i64
  (if (= (+ 1 2) 3) 1 0))

(defn test-string [] :i64
  (if (= (string-length "ab") 2) 1 0))
